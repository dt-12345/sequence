sequence ainb files for totk

`text/` is json format and `graphs/` are png graphs (the font used doesn't support japanese characters so those don't render properly)

`FormalSequence.root.ainb` is the main entry point